name: timisoara-netmob-light
channels:
  - conda-forge
dependencies:
  - python>=3.10
  - pandas>=2.0
  - numpy>=1.24
  - scikit-learn>=1.3
  - matplotlib>=3.7
  - seaborn>=0.13
  - pyarrow>=14.0
  - folium>=0.15
  - jupyter>=1.0
  - pyyaml>=6.0
  - pytest>=7.0
  - pip
  - pip:
      - xgboost>=2.0
      - python-docx>=1.1
      # Optional (modele secventiale + explainability):
      # - torch>=2.2
      # - shap>=0.45
