# Scripturi istorice

Nu fac parte din pipeline-ul principal (`scripts/run_pipeline.ps1`).

| Script | Notă |
|--------|------|
| `merge_gps_with_netmonster.py` | Prototip merge; înlocuit de `build_radio_gps_dataset` |
| `generate_simulated_gps.py` | Generator GPS (deja rulat; output în `data/raw/gps_simulated/`) |

Păstrate pentru referință / regenerare trasee dacă e nevoie.
